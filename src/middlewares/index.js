"use strict";

const checkCooldown = require("./checkCooldown");
const checkGlobalBan = require("./checkGlobalBan");
const checkTerms = require("./checkTerms");

module.exports = {
  checkCooldown,
  checkGlobalBan,
  checkTerms
};
